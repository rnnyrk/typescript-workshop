export default () => {
  /* 01
  * ======================================================
  * TODO: Extract the type shape to a type variable
  * ======================================================*/
  const company: { name: string } = { name: 'Label A' };
};
